name: Build Android APK
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del codice
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Prepara ambiente e cartella www
        run: |
          # Crea un package.json base se manca
          if [ ! -f package.json ]; then npm init -y; fi
          
          # Crea la cartella www
          mkdir -p www
          
          # Copia i file del gioco in www (escludendo file di sistema per evitare loop)
          rsync -av --exclude='www' --exclude='.github' --exclude='.git' --exclude='node_modules' . www/

      - name: Installazione dipendenze e Capacitor
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Inizializza Capacitor e Android
        run: |
          # Ora puntiamo Capacitor alla cartella pulita 'www'
          npx cap init "MioGioco" "com.esempio.gioco" --web-dir www
          npx cap add android
          npx cap copy android

      - name: Setup Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Build APK (Gradle)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Carica APK come Artefatto
        uses: actions/upload-artifact@v4
        with:
          name: gioco-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
